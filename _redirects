# Cloudflare Workers の静的アセットのリダイレクト
# 注意: Worker が処理するパス（/api/*）にはこのルールは効かない。
# ワイルドカードを含む行より上に、固定パスの行を置く（そのほうが速い）。

# 印刷物や口頭で伝えるときの短いパス
/demo               /app/                  302
/deck               /pitch/                302

# GitHub Pages では /-/ 配下に置かれていたので、その形で来ても拾う
/-/                 /                      301
/-/*                /:splat                301
